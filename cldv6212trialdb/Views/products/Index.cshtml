@model IEnumerable<cldv6212trialdb.product>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.item_descr_and_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.item_price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.item_image)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.item_descr_and_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.item_price)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.item_image)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
